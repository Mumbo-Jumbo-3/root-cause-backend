{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "alembic upgrade head && health-agent ingest && uvicorn health_agent.server:app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
